type ProjectContent = {
	title: string,
	description: string,
	url: string,
	imageUrl: string
}
export type ProjectModel = { title: string, subTitle: string, intro: string, contents: ProjectContent[] }
